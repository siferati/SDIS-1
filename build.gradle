apply plugin: 'java'
apply plugin: 'application'

mainClassName = "peer.Peer"

jar {
  manifest {
    attributes(
      'Main-Class': mainClassName
    )
  }
}

run {
  if(project.hasProperty('a')){
    args(a.split(','))
  }
}

javadoc {
  options.memberLevel = JavadocMemberLevel.PRIVATE
}
